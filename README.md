# GoKVStore

**Высокопроизводительное In-memory хранилище на Go, поддерживающее протокол Redis**

> Полноценный сервер, совместимый с официальными клиентами Redis и оптимизированный под экстремальные нагрузки. Благодаря инженерным оптимизациям достигнуто более **420 тысяч запросов в секунду** в pipeline режиме.

## 🚀 Ключевые фичи

### Tech Stack

* **Language**: Go 1.23+ (Goroutines, Unsafe)
* **Protocols**: RESP (Redis Serialization Protocol) - полная совместимость с `redis-cli` и любыми SDK
* **Concurrency**: 16 шардов с FNV-1a хешированием, RWMutex для read-heavy нагрузок
* **Memory Management**: Zero-copy parsing, buffer recycling, unsafe оптимизации (где безопасно)
* **Network**: TCP Pipelining для максимальной пропускной способности

## 📊 Performance Results

Тестирование проводилось с помощью встроенного бенчмарка и `redis-benchmark` на локальной машине (Go 1.23+, 10 параллельных клиентов).

| Mode | Operation | Throughput (RPS) | Latency (avg) | Latency (p99) |
|---|---|---|---|---|
| Request-Response | GET (10 clients) | **49,963** | 199.12µs | 836.9µs |
| Request-Response | SET (10 clients) | **~50,000** | 267.9µs | 997.1µs |
| TCP Pipelining | GET (batch=100) | **420,387** | 2.32ms | 4.02ms |
| TCP Pipelining | SET (batch=100) | **210,254** | 4.61ms | 8.86ms |

> **Key Achievement:** Благодаря инженерным оптимизациям удалось достичь более **420k RPS** для GET операций в pipeline режиме, что сопоставимо с промышленными решениями при аналогичных условиях.

### Тестовое окружение
- **Go Version**: 1.23+
- **Клиенты**: 10 параллельных соединений
- **Операций**: 100,000 запросов на тест
- **Pipeline Batch Size**: 100 команд
- **Тестирование**: Встроенный бенчмарк + `redis-benchmark`

**Примечание:** Высокие показатели RPS достигнуты на localhost, что позволило оценить эффективность алгоритмов парсинга и конкурентного доступа, минимизировав влияние сетевой задержки.

## 🛠 Эволюция производительности (How I Optimized It)

В ходе разработки проект прошел несколько стадий оптимизации, каждая из которых решала конкретную проблему производительности:

### 1. Борьба с Lock Contention (Sharding)

**Проблема:** Использование одного `sync.Mutex` на всю базу данных создавало очередь из горутин. При высокой нагрузке все операции блокировались друг другом, даже если работали с разными ключами.

**Решение:** Реализовано шардирование на 16 независимых сегментов с использованием FNV-1a хеширования. Это позволило горутинам работать с разными частями памяти параллельно, минимизируя contention.

**Результат:** Устранена блокировка между операциями над разными ключами, что критично для read-heavy нагрузок.

### 2. Zero-Copy & Unsafe (Memory Optimization)

**Проблема:** Стандартное приведение `string` → `[]byte` создает копию данных в памяти. При 300k+ запросах в секунду это создает миллионы аллокаций, нагружая Garbage Collector и увеличивая tail latency.

**Решение:** 
- Внедрен `unsafe.StringData()` и `unsafe.Slice()` для конвертации типов без копирования в критических участках (хеширование ключа в `GetShardIndex`)
- Кастомный парсер чисел `writeIntFast` вместо `strconv.Itoa`, работающий со статическим буфером на стеке, а не с кучей
- Использование `bufio.ReadSlice('\n')` вместо `ReadBytes` - данные остаются в буфере без копирования

**Результат:** Устранены миллионы аллокаций в секунду, снижена нагрузка на GC, улучшена производительность на 12-29% в зависимости от теста.

**⚠️ Важное решение:** Откат unsafe оптимизации в `ParseBulkString` из-за переиспользования буфера между командами. Это демонстрирует понимание границ применения unsafe и приоритет безопасности над производительностью в критических местах.

### 3. TCP Pipelining (Network Optimization)

**Проблема:** Сетевые задержки (RTT) ограничивали скорость до 25-50k RPS, так как клиент ждал ответа на каждый запрос перед отправкой следующего.

**Решение:** Реализована поддержка пакетной обработки команд. Клиент отправляет команды пачками без немедленного flush, сервер обрабатывает их одним циклом, минимизируя системные вызовы `read` и `write`.

**Результат:** Пропускная способность выросла с ~50k до **420k RPS** для GET операций - увеличение в **8.4 раза**.

### 4. Buffer Recycling & Slice Reuse

**Проблема:** Создание новых слайсов на каждую операцию (`make([]byte, length)`, `make([]string, 0, count)`) создавало нагрузку на GC.

**Решение:**
- Переиспользуемый буфер размером 64KB на соединение для чтения команд
- Переиспользование слайса аргументов команд через `args[:0]` с предварительно выделенной capacity

**Результат:** Минимизация работы Garbage Collector, снижение tail latency.

## 📁 Структура проекта

Проект следует стандартной структуре Go проекта (Standard Go Project Layout):

```
go-kv-store/
├── cmd/
│   └── server/          # Точка входа сервера
│       └── main.go
├── internal/
│   ├── handler/         # Обработка соединений и команд
│   │   └── handler.go
│   ├── resp/            # Парсинг и сериализация RESP протокола
│   │   └── resp.go
│   └── storage/          # Шардированное хранилище
│       └── shard.go
├── bench/                # Бенчмарк инструменты
│   ├── client.go
│   ├── benchmark.go
│   └── benchmark_test.go
├── go.mod
└── README.md
```

## 🏗️ Архитектура

```
┌─────────────┐
│   Client    │
│ (redis-cli) │
└──────┬──────┘
       │ RESP Protocol
       ▼
┌─────────────────────────────────┐
│      TCP Listener (6379)        │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│     RESP Parser (Zero-Copy)     │
│  - ReadSlice для чтения         │
│  - parseInt для чисел           │
│  - Buffer recycling             │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│    Command Handler              │
│  - Direct string comparison     │
│  - No ToUpper overhead          │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│   ShardStorage (16 shards)      │
│  ┌────────┐ ┌────────┐         │
│  │ Shard 0│ │ Shard 1│ ...      │
│  │ RWMutex│ │ RWMutex│         │
│  └────────┘ └────────┘         │
└─────────────────────────────────┘
```

## 🚦 Быстрый старт

### Требования
- Go 1.23.1 или выше
- (Опционально) Redis CLI для тестирования совместимости

### Установка и запуск

```bash
# Запустить сервер
go run ./cmd/server
```

Сервер запустится на `0.0.0.0:6379` и будет готов принимать подключения.

### Проверка через redis-cli

```bash
# Установить значение
redis-cli -p 6379 SET mykey "Hello World"

# Получить значение
redis-cli -p 6379 GET mykey
# Output: "Hello World"

# Проверить несуществующий ключ
redis-cli -p 6379 GET nonexistent
# Output: (nil)

# Ping команда
redis-cli -p 6379 PING
# Output: PONG
```

### Запуск бенчмарка

**Важно:** Сначала запустите сервер в отдельном терминале!

```bash
# Запустить встроенный бенчмарк (включая pipeline тесты)
# Убедитесь, что сервер запущен на localhost:6379
go run -tags benchmark ./bench

# Или использовать стандартный redis-benchmark
redis-benchmark -h localhost -p 6379 -n 100000 -c 50 -t set,get

# Тест pipeline режима через redis-benchmark
redis-benchmark -h localhost -p 6379 -n 1000000 -c 50 -t set,get -P 100
```

**Примечание:** Флаг `-P 100` в `redis-benchmark` включает pipeline режим с размером батча 100 команд, что позволяет достичь максимальной пропускной способности.

## 📝 Поддерживаемые команды

| Команда | Описание | Пример |
|---|---|---|
| `SET key value` | Установить значение ключа | `SET name "John"` |
| `GET key` | Получить значение ключа | `GET name` |
| `PING` | Проверка соединения | `PING` |
| `QUIT` / `EXIT` | Закрыть соединение | `QUIT` |
| `CONFIG GET` | Совместимость с redis-benchmark | `CONFIG GET *` |

## 🔬 Технические детали

### Sharding Strategy
- **Количество шардов**: 16 (константа `ShardCount`)
- **Распределение**: `hash(key) % ShardCount`
- **Блокировки**: `RWMutex` на каждый шард для оптимизации read-heavy нагрузок
- **Результат**: Минимизация lock contention, параллельная обработка операций над разными ключами

### RESP Protocol Implementation
- Полная поддержка RESP массивов (`*N\r\n`)
- Bulk strings (`$N\r\n`)
- Simple strings (`+OK\r\n`)
- Errors (`-ERR message\r\n`)
- Null bulk strings (`$-1\r\n`)
- Совместимость с `redis-cli`, `redis-benchmark` и любыми Redis SDK

### Memory Management & Optimizations
- **Read Buffer**: 64KB переиспользуемый буфер на соединение (buffer recycling)
- **Write Buffer**: `bufio.Writer` с размером буфера по умолчанию (4KB)
- **Args Slice**: Переиспользуемый слайс для аргументов команд (capacity: 16, reset через `[:0]`)
- **Unsafe Optimizations**: `unsafe.StringData()` для хеширования ключей (безопасно, т.к. данные используются сразу)
- **Custom Integer Parser**: `writeIntFast` - запись чисел в буфер без создания строк

### Performance Optimizations Summary
1. ✅ **Sharding**: Устранение lock contention между операциями
2. ✅ **Zero-Copy Parsing**: `ReadSlice` вместо `ReadBytes`, прямой парсинг из буфера
3. ✅ **Unsafe для временных данных**: Оптимизация хеширования без копирования
4. ✅ **Buffer Recycling**: Переиспользование буферов и слайсов
5. ✅ **TCP Pipelining**: Пакетная обработка команд для минимизации RTT
6. ✅ **Custom Integer Writer**: Запись чисел без аллокаций строк