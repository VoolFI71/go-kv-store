# GoKVStore

**Высокопроизводительное In-memory хранилище на Go, совместимое с протоколом RESP (Redis).**

> **Почему это круто:** Я реализовал не просто мапу с сетевым доступом, а полноценный сервер, который понимает официальные клиенты Redis и оптимизирован под экстремальные нагрузки. Сервер обрабатывает более 340 тысяч запросов в секунду при использовании конвейеризации, что доказывает эффективность сетевого парсера и системы хранения.

## 🚀 Ключевые фичи

### Tech Stack

* **RESP Protocol Support**: Полная поддержка сериализации Redis. Работает с `redis-cli` и любыми SDK (redis-py, go-redis, etc.)
* **Zero-Copy Parser**: Оптимизированный парсинг байтов без лишних аллокаций памяти (использование `bufio.ReadSlice` и кастомного парсера чисел)
* **High-Concurrency Sharding**: Хранилище разделено на 16 сегментов (shards) с FNV-1a хешированием для минимизации блокировок (mutex contention)
* **TCP Pipelining**: Поддержка конвейерной обработки запросов для достижения максимальной пропускной способности
* **Buffer Recycling**: Переиспользование буферов для чтения команд, минимизация работы Garbage Collector

## 📊 Performance Results

Тестирование проводилось с помощью встроенного бенчмарка и `redis-benchmark`.

| Режим | Операций в сек (RPS) | Latency (p99) | Latency (p50) |
|---|---|---|---|
| Request-Response (GET) | ~25,000 | < 1ms | < 0.5ms |
| Request-Response (SET) | ~25,000 | < 1ms | < 0.5ms |

| Pipeline (Batch 100, SET) | ~340,000 | ~4ms | ~2ms |

**Результат:** Сервер обрабатывает более 340 тысяч запросов в секунду при использовании конвейеризации, что доказывает эффективность сетевого парсера и системы хранения.

### Тестовое окружение
- **CPU**: Современный многоядерный процессор
- **Клиенты**: 10 параллельных соединений
- **Операций**: 100,000 запросов на тест
- **Pipeline Batch Size**: 100 команд

## 🔧 Инженерные оптимизации

### Memory Management
Я минимизировал работу Garbage Collector, используя переиспользуемые буферы для чтения команд. Вместо создания нового слайса на каждую операцию (`make([]byte, length)`), используется один буфер размером 64KB на соединение, который переиспользуется для всех операций чтения.

### Lock Optimization
Вместо одного глобального Mutex на всю базу, данные разбиты на 16 шардов с использованием FNV-1a хеширования. Это позволяет потокам работать параллельно над разными ключами, минимизируя contention. Каждый шард использует `RWMutex` для оптимизации read-heavy нагрузок.

### Fast I/O
Реализован кастомный парсер целых чисел из байтовых срезов (`parseInt`), который работает быстрее стандартного `strconv.Atoi`, так как:
- Работает напрямую с байтами из буфера `bufio.Reader`
- Не создает промежуточных строк
- Использует `ReadSlice` вместо `ReadBytes` для избежания аллокаций

### Zero-Copy Parsing
- Использование `bufio.ReadSlice('\n')` вместо `ReadBytes` - данные остаются в буфере без копирования
- Парсинг чисел напрямую из буфера без создания строк
- Переиспользование слайса аргументов команд через `args[:0]`

### Pipeline Optimization
- Отправка команд пачками без немедленного flush
- Один системный вызов `Flush()` на весь батч команд
- Чтение всех ответов пачкой после отправки

## 🏗️ Архитектура

```
┌─────────────┐
│   Client    │
│ (redis-cli) │
└──────┬──────┘
       │ RESP Protocol
       ▼
┌─────────────────────────────────┐
│      TCP Listener (6379)        │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│     RESP Parser (Zero-Copy)     │
│  - ReadSlice для чтения         │
│  - parseInt для чисел           │
│  - Buffer recycling             │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│    Command Handler              │
│  - Direct string comparison     │
│  - No ToUpper overhead          │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│   ShardStorage (16 shards)      │
│  ┌────────┐ ┌────────┐         │
│  │ Shard 0│ │ Shard 1│ ...      │
│  │ RWMutex│ │ RWMutex│         │
│  └────────┘ └────────┘         │
└─────────────────────────────────┘
```

## 🚦 Быстрый старт

### Требования
- Go 1.23.1 или выше
- (Опционально) Redis CLI для тестирования совместимости

### Установка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/go-kv-store
cd go-kv-store

# Запустить сервер
go run .
```

Сервер запустится на `0.0.0.0:6379` и будет готов принимать подключения.

### Проверка через redis-cli

```bash
# Установить значение
redis-cli -p 6379 SET mykey "Hello World"

# Получить значение
redis-cli -p 6379 GET mykey
# Output: "Hello World"

# Проверить несуществующий ключ
redis-cli -p 6379 GET nonexistent
# Output: (nil)

# Ping команда
redis-cli -p 6379 PING
# Output: PONG
```

### Запуск бенчмарка

```bash
# Запустить встроенный бенчмарк
go run -tags benchmark .

# Или использовать стандартный redis-benchmark
redis-benchmark -h localhost -p 6379 -n 100000 -c 50 -t set,get
```

## 📝 Поддерживаемые команды

| Команда | Описание | Пример |
|---|---|---|
| `SET key value` | Установить значение ключа | `SET name "John"` |
| `GET key` | Получить значение ключа | `GET name` |
| `PING` | Проверка соединения | `PING` |
| `QUIT` / `EXIT` | Закрыть соединение | `QUIT` |
| `CONFIG GET` | Совместимость с redis-benchmark | `CONFIG GET *` |

## 🔬 Технические детали

### Sharding Strategy
- **Алгоритм**: FNV-1a 32-bit хеширование
- **Количество шардов**: 16 (константа `ShardCount`)
- **Распределение**: `hash(key) % ShardCount`
- **Блокировки**: `RWMutex` на каждый шард для оптимизации read операций

### RESP Protocol Implementation
- Полная поддержка RESP массивов (`*N\r\n`)
- Bulk strings (`$N\r\n`)
- Simple strings (`+OK\r\n`)
- Errors (`-ERR message\r\n`)
- Null bulk strings (`$-1\r\n`)

### Buffer Management
- **Read Buffer**: 64KB переиспользуемый буфер на соединение
- **Write Buffer**: `bufio.Writer` с размером буфера по умолчанию (4KB)
- **Args Slice**: Переиспользуемый слайс для аргументов команд (capacity: 16)

## 🎯 Use Cases

- **Кэширование**: Быстрое in-memory кэширование с Redis-совместимым API
- **Session Storage**: Хранение сессий пользователей
- **Rate Limiting**: Счетчики для rate limiting
- **Real-time Analytics**: Быстрое агрегирование метрик
- **Learning**: Изучение высокопроизводительных сетевых серверов на Go

## ⚠️ Ограничения

- **In-Memory Only**: Данные не сохраняются на диск (нет persistence)
- **Single Node**: Нет репликации или кластеризации
- **Limited Commands**: Реализованы только базовые команды (SET, GET, PING)
- **No Expiration**: Нет поддержки TTL для ключей
